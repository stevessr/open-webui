# This file is intentionally left blank.
# All backend dependencies are now managed by the `pyproject.toml` file
# in the root of the project.
#
# To install core dependencies, run `pip install .` from the root directory.
# To install optional features, use extras, e.g.:
# `pip install .[postgres,chromadb]`
# `pip install .[all]`